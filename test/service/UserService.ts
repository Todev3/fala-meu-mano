import { test, describe } from "vitest";

describe("Test getOrCreateUser", () => {
  test("should work", async () => {
    // const user = getOrCreateUser("", mockRep);
    // expect(user).toEqual(null);
  });
});
